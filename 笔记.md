从主函数看，这个项目本质上是在做一件事：Appium 负责“看见和操作 Android 页面”，GPT 负责“理解当前页面该去哪、能填哪些问卷项”，WorkflowRunner 负责把这两者编排成一个可恢复、可回放、可探索的自动化流程。

主入口在 main.py (line 122)。main() 做了 5 件事：

解析命令行参数，确定目标包名、问卷目录、模型、预算等。
加载问卷定义，生成运行时状态对象 QuestionnaireState，见 questionnaire_state.py (line 186)。
初始化 Appium 连接，得到设备操作客户端 AndroidAppiumClient，见 appium_android.py (line 134)。
初始化 GPTClient，里面封装了导航、问卷填充、恢复三类 LLM 调用，见 gpt_cls.py (line 500)。
组装 WorkflowRunner 并调用 runner.run(task=args.task)，见 main.py (line 152) 和 workflow.py (line 753)。
你可以把 WorkflowRunner.run() 理解成“总调度循环”。它启动后先确保目标 App 在前台，然后抓取第一帧权威快照 snap，并生成当前页面签名 state_sig，这一步在 workflow.py (line 778) 和 workflow.py (line 1468)；快照来源是 appium.capture_snapshot()，会一次性取到稳定的 XML、截图和设备信息，见 appium_android.py (line 267)。随后流程会把 XML 转成内部 UI 树，做后处理、缓存、状态签名计算，所以后续所有判断都围绕这个 snap/state_sig 展开。

进入主循环后，每一轮大致都是这个顺序，核心在 workflow.py (line 796)：

先 _drain_futures()，把已经完成的 LLM 异步结果收回来，尤其是问卷更新会立刻写入 QuestionnaireState，见 workflow.py (line 2301)。
调 open_gaps() 看当前还有哪些“可见但未回答”的问卷项，见 questionnaire_state.py (line 418)。如果没有 gap 就结束。
检查当前前台包名是不是目标 App；如果跑到浏览器、广告页、系统页，就先恢复。
做 drift 检查，也就是“我没点东西，但页面是不是自己变了”；变了就重新规划。
等导航模型 LLM1 的结果，得到当前页候选动作、overlay 类型、返回方式等；如果超时才退回启发式动作。LLM1 调用入口在 gpt_cls.py (line 500)。
如果识别到 overlay，比如弹窗或 loading，就优先处理，不直接瞎点。
对候选动作先做 probe，也就是“试探一下再退回来”，拿证据判断这个动作会到哪里、是不是新页面、会不会带来问卷收益。
基于 probe 结果选择一个真正的 forward 动作提交执行；如果当前页探索完了，会回溯到祖先页面，或者走全局 beam search 去别的更有价值的页面。
每次动作后重新抓权威快照，记录状态迁移图，再进入下一轮。
这里有个很关键的设计：LLM 不是串行阻塞地“想到哪算哪”，而是被异步调度的。_schedule_state() 会在进入新页面时，同时安排：

LLM1 propose_navigation：给当前页提导航动作。
LLM2-1 propose_topic_routes：判断当前页面和哪些问卷 topic 相关。
LLM2-2 propose_topic_fill：只针对相关 topic 生成问卷答案更新。
见 workflow.py (line 2167)。这说明项目的目标不是单纯自动点 UI，而是“边探索页面边补问卷”。
问卷状态这一层也值得你先理解。QuestionnaireState.load_from_dir() 会把 questionnaire 或 questionnaire-v2 里的 JSON 全部加载成内存结构，支持题目、选项、显隐依赖、topic 分组等，见 questionnaire_state.py (line 186)。apply_updates() 则负责把 LLM 的填空结果真正落到状态里，而且不是简单覆盖，而是带规则的：

单选题需要更高置信度才能覆盖旧答案。
多选题默认只做并集，不轻易删除旧选项。
gate 题如果已经推断成 Yes，不会被低置信度改回 No。
子题命中后还能反推父题。
见 questionnaire_state.py (line 750)。所以这个系统本质上是“保守累积证据”的。
GPTClient 这层你可以先按 3 类能力来记：

propose_navigation()：告诉 workflow 这一页能点什么、哪个更值得探索、如何返回。
propose_topic_routes() / propose_topic_fill()：告诉问卷系统这一页和哪些题有关，以及能填哪些答案。
recover_state()：迷路、卡死、overlay 处理失败时给恢复动作建议。
见 gpt_cls.py (line 500)、gpt_cls.py (line 577)、gpt_cls.py (line 603)、gpt_cls.py (line 641)。
如果你刚接手，我建议你先优先建立这几个认知：

main.py 只是装配，真正逻辑基本都在 workflow.py。
WorkflowRunner 的输入不是“原始 XML”，而是 _capture_and_process() 产出的标准化快照。
项目的核心目标不是遍历页面，而是“为了填问卷而探索页面”。
问卷状态是强约束中心，很多导航决策都受 open_gaps() 和 active topics 驱动。
当前 main.py 里有一段直接改写 sys.argv 的开发用默认参数，见 main.py (line 107)。这意味着它现在更像本地调试入口，不完全是干净的生产 CLI。


