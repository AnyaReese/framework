# 更新日志

## 2026-05-04

### 本次配置更新

- 更新 `.gitignore`，新增结果目录忽略规则：
  - `4-30-debug/runs/`
  - `5.2/download/`
  - `5.2/results/`

### 本次代码功能更新（相对 `origin/ruogan..HEAD`）

- 重写 `LLM1` 导航系统提示词（`gpt_cls.py`）：
  - 从概念性说明升级为字段级/流程级约束；
  - 强化 `overlay_kind` 判定策略；
  - 强化 `candidate_actions` 与 `return_method/return_actions` 约束；
  - 目标是提升动作提议的稳定性与可执行性。
- 新增两个调试探针脚本：
  - `4-30-debug/current_page_probe.py`
  - `4-30-debug/secure_capture_probe.py`
  - 用于复现并分类截图失败问题（如 secure flag），并输出结构化报告。
- 调整本地调试默认包名（`main.py`、`mytest2/debug_run_llm1.py`）：
  - 默认调试目标切换到网易相关包名，用于当前测试场景。

