# BACK 后置前台校验修复说明（2026-05-03）

## 背景

在探索流程中，`BACK` 动作执行前有前台包校验，但执行后没有立即校验。  
因此在某些页面（登录/外跳/系统页）按下 `BACK` 后，可能已经退到非目标 App（如桌面或外部页面），随后被 `_capture_and_process()` 记录为“异常前台快照”。

## 本次改动目标

只做最小修复：为 `BACK` 增加**后置前台校验**，并在 probe-return/probe 失败路径中联动处理，避免继续沿错误前台执行。

## 修改文件

- `F:\workplace\framework\workflow.py`

## 具体改动

### 1) `_execute_action` 的 `ActionType.BACK` 分支增加后置前台校验

- 位置：`workflow.py` 中 `ActionType.BACK` 分支（原先 `self.appium.back()` 后直接 `return True`）
- 现在行为：
  1. 执行 `self.appium.back()`
  2. 立即调用 `_foreground_is_allowed()`
  3. 若前台不允许：
     - 记录事件：`foreground_mismatch_during_action`（新增字段 `phase="post_back"`）
     - 设置 `last_action_failure.reason = "post_back_foreground_mismatch"`
     - 尝试 `ensure_foreground(target_package, target_activity)` 拉回
     - 该动作返回 `False`（不再当作成功）
  4. 若前台允许，保持原行为返回成功

### 2) `_return_to_expected` 的 BACK fallback 增加新失败短路

- 位置：`back_fallback_primary` 与 `back_fallback_last_resort` 两处
- 新增判断：
  - 若 `last_action_failure.reason == "post_back_foreground_mismatch"`，立即 `return False`
- 目的：避免在已跑飞时继续重复 BACK。

### 3) `_probe_candidates` 的动作失败分支扩展“状态级故障”识别

- 位置：`_execute_action_sequence` 失败后的处理分支
- 原逻辑仅识别 `foreground_mismatch`
- 现扩展为同时识别：
  - `foreground_mismatch`
  - `post_back_foreground_mismatch`
- 处理方式：统一走抓新快照并 `forced_replan`，避免把该异常当作普通候选失败继续执行。

## 行为变化总结

- 之前：`BACK` 后可能退到外部前台，但该动作仍被判成功，后续才在别处兜底。
- 现在：`BACK` 后若前台跑飞，当场判失败并尝试拉回，随后让上层直接重规划。

## 风险与兼容性

- 改动范围局限于 `BACK` 路径，未修改恢复总框架、重启预算、`RecoveryReason` 枚举。
- 可能看到更多 `foreground_mismatch_during_action` 日志（这是预期，代表更早识别问题）。

## 回归建议

1. 复现你之前的“BACK 后前台跑飞”场景，确认：
   - 出现 `phase=post_back` 的 mismatch 事件
   - 不再持续沿错误前台继续探测
2. 跑一个普通无外跳 App，确认 `BACK` 正常路径不受影响。
3. 对比前后 trace：
   - 非目标前台截图数量
   - 无效恢复/重启次数

