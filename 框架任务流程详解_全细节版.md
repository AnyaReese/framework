# Framework 框架任务流程详解（全细节版）

本文档基于当前代码实现，完整描述一次 `run` 执行时从启动到停止的全链路流程，重点覆盖：
- UI 状态图（StateGraph）如何维护
- DFS 遍历策略如何落地
- LLM 每一步具体做了什么

---

## 1. 总体架构与目标

### 1.1 目标

框架目标不是“盲点页面”，而是：
1. 在 App 内稳定探索 UI（导航、回退、重启回放）。
2. 并行调用 LLM 分析当前页面语义与问卷匹配关系。
3. 把探索过程维护成可复用的 **UI 状态转移图**（StateGraph）。
4. 在预算内尽可能覆盖更多有效状态并产出问卷观测结果。

### 1.2 核心模块分工

1. 入口与组装：`main.py`
- 参数解析、日志、元数据分析、构造 `WorkflowRunner`。

2. 执行调度：`workflow.py`
- 主循环、快照处理、LLM 调度、动作执行、恢复与回放、DFS/beam。

3. UI 状态图：`state_graph.py`
- 节点、边、验证统计、最短路径回放。

4. LLM 适配层：`gpt_cls.py`
- LLM1（导航）、LLM2-1（router）、LLM2-2（blocks fill）、LLM3（恢复）。

5. 问卷状态层：`questionnaire_state2.py`
- router/block 数据装载、block 匹配、运行计数、观测落盘。

---

## 2. 启动阶段（main.py）

对应入口：`main.py::main()`。

### 2.1 参数与预算

`parse_args()` 提供 `run` 子命令关键参数：
1. 设备与目标：`--appium-url` `--device-name` `--package` `--activity`
2. 问卷与元数据：`--questionnaire-dir` `--questionnaire-type-source` `--metadata-csv-path`
3. 预算：`--time-budget` `--max-actions` `--probe-cap` `--workers`
4. 行为策略：`--disable-probe-return` `--min-candidate-score`
5. 跟踪：`--trace-dir` `--run-id`

这些参数最终汇总到 `BudgetConfig` 和 `WorkflowRunner` 初始化中。

### 2.2 元数据预分析（可选）

当设置 `metadata_csv_path` 时：
1. 从 CSV 按 `appId == package` 找当前 App。
2. 调用 `GPTClient.analyze_app_metadata()`。
3. 产出 `app_intro`、`focus_hints`、`questionnaire_type` 建议。
4. 结合 `questionnaire_type_source` 决定最终问卷目录。

### 2.3 构建执行对象

1. `AndroidAppiumClient.init_connection()` 初始化设备连接。
2. `QuestionnaireState2.load_from_questionnaire_dir(...)` 装载 router/block。
3. `GPTClient(...)` 初始化 API 客户端与 token 统计器。
4. `WorkflowRunner(...)` 组装所有运行态依赖。

然后调用 `runner.run(task=...)` 进入主流程。

---

## 3. WorkflowRunner 的运行态数据模型

`workflow.py::WorkflowRunner` 内部维护了几组关键状态。

### 3.1 图与遍历状态

1. `graph: StateGraph`
- 全局 UI 状态转移图。

2. `dfs_stack`, `dfs_via`, `parent_map`
- 当前 DFS 路径栈、入边动作、父节点关系。

3. `state_candidates`
- 每个 family 下 NAV 候选动作集合。

4. `attempted_actions`, `explored_actions`
- 动作“尝试过”和“探索完成”集合（按 family_id 记录）。

5. `probe_outcomes`, `probe_novelty`
- 候选探测后的目标状态、首次发现新状态标记。

### 3.2 LLM 管道状态

1. futures
- `_nav_futures`, `_block_router_futures`, `_blocks_fill_futures`, `_topic_route_futures`, `_topic_fill_futures`。

2. caches
- `nav_cache`, `block_router_cache`, `blocks_fill_cache` 等。

3. 节流/稳定性
- `nav_failures`, `nav_cooldown_until`, `topic_fill_attempt_ts`。

### 3.3 快照与身份状态

1. `snapshot_cache`
- `(xml_hash, screenshot_hash, scale, fg pkg/activity, mode)` -> `(uist, vid_map)`。

2. `sig_to_family`
- `state_sig -> struct_sig`，用于 family 归并。

3. `visual_state_registry`
- 视觉状态历史（phash 路径匹配）。

---

## 4. 权威快照链路（_capture_and_process）

对应函数：`workflow.py::_capture_and_process()`。

这是全局最重要的数据入口，后续所有决策都基于它。

### 4.1 输入与采集

1. 调 `appium.capture_snapshot()` 同步拿到：
- 原始 XML
- 原始截图（base64）
- device_info

2. 如缺 hash，则本地补算 `xml_hash_raw`、`screenshot_hash_raw`。

### 4.2 归一化与稳定化

1. 坐标比例推断 `_infer_coord_scale(...)`。
2. 前台上下文读取：`foreground_package/activity`。
3. 状态栏区域裁剪：
- 截图 top mask
- XML 顶部节点预处理

### 4.3 UI 树构建与后处理

1. `parse_xml_to_uist(xml, pixel_ratio)`。
2. 判断 `xml_reliable`。
3. 分支后处理：
- `BaseUI.post_process_ui(...)`（xml可靠）
- `BaseUI.post_process_ui_uied_first(...)`（xml不可靠）

输出：
- `uist`
- `vid_map`（element_id -> 节点）

### 4.4 状态签名体系

同一快照会生成 4 类签名：
1. `state_sig`：主签名（缓存键、图节点键）。
2. `struct_sig`：结构签名（family 归并）。
3. `coarse_sig`：粗粒度签名（更稳）。
4. `fine_sig`：细粒度签名（更区分）。

还会生成 `screenshot_phash`，供视觉身份匹配。

### 4.5 身份判定（xml/phash）

`_resolve_state_identity(...)` 会综合：
- xml_reliable
- xml_state_sig
- struct_sig
- screenshot_phash

最终给出当前 `state_sig` 及 identity_source（xml 或 phash）。

---

## 5. UI 状态图维护机制（重点）

对应实现：`state_graph.py` + `workflow.py` 的 `_graph_*` 封装。

### 5.1 节点与边结构

1. Node
- `sig`
- `visit_count`
- `first_ts` / `last_ts`
- `overlay_kind`
- `meta`
- `outgoing` / `incoming`

2. Edge
- `src` -> `dst`
- `action`（统一存成 `{"actions":[...]}`）
- `count`
- `no_effect`
- `verified_ok` / `verified_fail` / `last_verified_ts`

### 5.2 三种写入方式

1. `record_observation(sig, ...)`
- 用于“没有明确前置动作”的到达场景：
  - 入口
  - 重启后落点
  - 恢复后对齐
- 会 `touch(sig)`，`visit_count +1`。

2. `record_transition(src, dst, action, touch=True|False, ...)`
- 用于“明确动作导致的转移”。
- `touch=True` 时会触达 src/dst 并加 visit。
- `touch=False` 用于 probe 等高频边，避免污染 visit 统计。

3. `annotate(sig, ...)`
- 仅更新 metadata/overlay，不增加 visit_count。

### 5.3 新状态判定语义

关键语义：`dst_was_new_at_discovery`。

实现上必须先判断 `dst not in nodes`，再 add_edge/touch。
否则会因 touch 先创建节点导致“首次发现”判定错误。

`workflow.py::_graph_record_transition(...)` 严格依赖这个返回值更新 `no_new_state_count`。

### 5.4 边验证统计

在 replay / graph 导航时，执行边会写：
- `record_edge_verification(..., ok=True|False)`。

这会反哺 beam cost 的边惩罚（unverified/stale edge penalty）。

---

## 6. 主循环时序（run）

对应函数：`workflow.py::run(task)`。

每轮 loop 基本顺序：

1. 停止条件检查 `_stop_condition_reason(...)`
- time budget
- max actions
- saturation / stall

2. 回收异步任务 `_drain_futures()`
- NAV、router、blocks_fill、recovery 等结果入缓存
- observation 落盘
- 统计更新

3. 前台守门 `_handle_foreground_gate(...)`
- 非目标包则尝试 deterministic recovery / relaunch 拉回

4. 漂移检测 `_preflight_refresh_if_changed(...)`
- 无动作情况下发现 UI 变化，记录 drift 边并强制 replan

5. NAV barrier `_wait_nav_or_fallback(...)`
- 优先等待 LLM1
- 等待期间也做 drift 检测
- 超时后才允许 heuristics

6. overlay 分流
- dismiss：先按 LLM dismiss actions
- loading：bounded wait
- unresolved：走 recovery -> restart+replay

7. 候选处理
- probe（若开启）
- forward 选择 `_choose_forward(...)`
- 执行动作 `_execute_action_sequence(...)`

8. 后置快照与转移写图
- capture next snapshot
- `record_transition(src,dst,action)`
- DFS 入栈/对齐
- schedule 新状态

9. 如果无 forward
- exhausted -> DFS 回溯
- 无祖先可回 -> global frontier / beam / restart+replay

---

## 7. DFS 遍历策略（重点）

### 7.1 状态是否 exhausted

对应：`_is_state_exhausted(sig)`。

逻辑：
1. 取 family_id。
2. 获取该 family 的候选动作（`state_candidates` 或 `nav_cache`）。
3. 候选 key 集合与 `explored_actions[fam]` 对比。
4. 全部候选已 explored -> exhausted。

注意：
- `attempted != explored`。
- 只有确认完成/无效的分支才会标 explored。

### 7.2 回溯策略

`_nearest_ancestor_with_remaining_work(cur_sig)`：
- 从 `dfs_stack` 倒序找最近未 exhausted 的祖先。
- 找到则 `_backtrace_to(target)`。

若祖先全 exhausted：
- `_pick_global_frontier()` 在全图挑未完成高价值节点。
- 尝试 `_navigate_via_graph()` 或 `_restart_and_replay()`。

### 7.3 beam 全局切换

`_maybe_beam_switch(...)` 在“本地 forward 前”评估是否全局跳转。

核心包含：
1. Value（novelty、visit、yield、topic match、ui_type bonus）
2. Cost（路径步数、重启惩罚、边验证惩罚）
3. 约束（cooldown、commit window、min_switch_gain）

满足收益阈值才切换，避免抖动。

---

## 8. LLM 交互链路（重点）

对应类：`gpt_cls.py::GPTClient`。

### 8.1 LLM1：导航（propose_navigation）

输入：
1. screenshot
2. ui_digest（压缩 UI）
3. block_status
4. task/history/state_sig
5. app_intro/focus_hints

输出：`NavigationProposal`
1. `overlay_kind` / `overlay_reason`
2. `overlay_dismiss_actions`
3. `candidate_actions`（每个可含多步 actions）
4. `return_method` / `return_actions`
5. `page_summary` / `ui_type` / `page_tags`

用途：
- 决定是否先处理 overlay
- 提供 forward/probe 候选
- 提供语义标签给后续 scoring

### 8.2 LLM2-1：router（propose_router_answers）

输入：
1. screenshot
2. router_questions（来自 questionnaire_routers.json）
3. app_intro/focus_hints
4. state_sig

输出：`RouterResult.router_updates`

本地后处理（不是 LLM）：
1. `match_blocks_from_router_answers(...)`
2. `mark_blocks_hit(...)`
3. 对命中 blocks 调度 LLM2-2

### 8.3 LLM2-2：blocks fill（propose_blocks_fill）

输入：
1. screenshot
2. matched blocks payload
3. app_intro/focus_hints
4. state_sig

输出：`BlocksFillResult`
- 每个 block 的 `proposed_updates`

后处理：
1. `mark_blocks_visited(...)`
2. 更新 `state_update_counts`
3. observation 落盘

### 8.4 LLM3：恢复（recover_state）

触发场景：
- overlay unresolved
- stuck
- backtrace failed
- capture failed 等

输入：
1. screenshot
2. ui_digest
3. frontier_hint/last_nav/note/state_sig

输出：`RecoveryProposal.candidate_actions`

执行策略：
- deterministic recovery 优先
- LLM recovery 作为补充
- 失败再升级到 restart+replay

### 8.5 结构化调用封装（_call_structured）

流程：
1. 优先 `beta.chat.completions.parse` + Pydantic schema。
2. 若失败，fallback 到普通 completion + JSON 抽取。
3. 内置 rate-limit 重试。
4. 记录 token usage（总量 + 按操作分解）。

---

## 9. 问卷状态层（questionnaire_state2）

### 9.1 数据加载

`load_from_questionnaire_dir(...)` 会定位 split 目录并读取：
1. `questionnaire_routers.json`
2. `questionnaire_blocks.json`

初始化：
- `routers`
- `blocks`
- `block_status`

### 9.2 运行时匹配

`match_blocks_from_router_answers(...)`：
- 把 router 回答归一化为 lookup
- 校验每个 block 的 `block_show_if`
- 返回当前 UI 命中的 block 列表

### 9.3 计数与观测

1. `mark_blocks_hit`：被命中次数。
2. `mark_blocks_visited`：送去 fill 次数。
3. `save_observation`：把 router/fill 结果存成 JSON。

---

## 10. 失败恢复与重启回放

### 10.1 recover 流程

`_recover(...)` 的层级：
1. deterministic ladder（back/wait/前台校正等）
2. LLM3 proposal
3. 仍失败 -> restart+replay

### 10.2 replay 流程

`_restart_and_replay(...)`：
1. 重启 app 回 entry/restart_entry
2. 从 `StateGraph.shortest_action_path(src,dst)` 找动作路径
3. 执行动作并做 divergence check
4. 写边验证统计（ok/fail）

---

## 11. 可观测输出与分析产物

### 11.1 trace 输出

若启用 `JsonlTraceCallbacks`，每次 run 会输出：
1. `trace.jsonl`
2. `screens/`、`screens_raw/`
3. `xml/`、`xml_raw/`
4. `uist/`
5. `screens_annotated/`
6. `observations/`（router/nav/blocks_fill）

### 11.2 analysis 输出

`_export_analysis_snapshot(...)` 在 stop / run_exit 导出：
1. `state_graph_snapshot.json`
2. `dfs_snapshot.json`
3. `state_action_snapshot.json`
4. `run_analysis_summary.json`

### 11.3 可视化

`visualize_run_analysis.py` 读取上述分析文件（或从 trace 重建）生成：
1. `ui_transition_graph.png`
2. `dfs_stack.png`
3. `report.md`

---

## 12. 一次 run 的“真实语义”总结

从系统设计上，这个框架不是单纯 BFS/DFS 点击器，而是：
1. 以快照签名为核心的状态机探索器。
2. 以 StateGraph 累积可回放经验。
3. 用 DFS 保证局部分支完整性。
4. 用 beam 与 replay 实现全局重定位。
5. 用 LLM1/2/3 分别承担导航、问卷理解、异常恢复。

因此它的关键价值在于：
- 可解释（trace + 图）
- 可恢复（recovery + replay）
- 可迭代（签名/策略/提示词都可持续优化）

