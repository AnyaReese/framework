# Framework 项目工作流程说明（详细版）

本文档用于说明 `F:/workplace/framework` 这个项目在一次完整运行中，整体会经历哪些步骤，以及每个步骤在代码里是怎么实现的。

## 1. 项目目标与核心思路

这个项目的目标是：
- 使用 Appium 控制 Android 应用，持续探索页面。
- 在探索过程中调用多轮 LLM（导航、问卷路由、问卷填充）分析页面。
- 把“页面状态 + 动作转移”记录成可回放的状态图（StateGraph）。
- 逐步收集问卷相关证据，输出观测数据（trace + observation）。

核心思路是“快照驱动 + 异步分析 + 状态图探索”：
- 任何决策都尽量基于权威快照（XML + 截图）做。
- LLM 调用走线程池异步，避免阻塞动作执行。
- 探索过程维护 DFS 栈 + 全局图，支持回退、恢复、重启后回放。

---

## 2. 启动阶段（参数解析与组件初始化）

对应入口：`main.py` 中 `main()`。

### 2.1 解析运行参数

`parse_args()` 定义了多类命令：
- `run`：主流程入口。
- `list-packages` / `list-processes` / `screenshot` / `dump-ui` / `install-apk`：设备工具命令。

`run` 常用参数分几组：
- 设备与目标应用：`--appium-url`、`--device-name`、`--package`、`--activity`。
- 问卷输入：`--questionnaire-dir`、`--questionnaire-type-source`、`--metadata-csv-path`。
- 预算与探索：`--time-budget`、`--max-actions`、`--probe-cap`、`--disable-probe-return`。
- 模型：`--model`、`--temperature`、`--timeout`、`--api-key`。
- 跟踪与调试：`--trace-dir`、`--run-id`、`--pause`、`--interactive-debug`。

### 2.2 日志与环境

- `setup_logging()` 同时配置控制台和 `app.log` 文件日志。
- 启动时 `load_dotenv()` 加载 `.env`。
- 若未提供 API key，会给出警告。

### 2.3 元数据预分析（可选）

如果设置了 `--metadata-csv-path`：
1. 读取 CSV（支持多种编码）。
2. 用 `appId` 匹配当前包名。
3. 调用 `GPTClient.analyze_app_metadata()`，得到：
   - `app_intro`
   - `focus_hints`
   - 建议的问卷类型（`games/social_apps/others`）
4. 根据 `questionnaire_type_source` 决定最终使用哪个问卷目录。

### 2.4 初始化三大核心对象

- `AndroidAppiumClient(...).init_connection()`：设备连接与动作执行层。
- `QuestionnaireState2.load_from_questionnaire_dir(...)`：加载 router/block 结构化问卷。
- `GPTClient(...)`：封装所有结构化 LLM 调用。

然后组装：
- `BudgetConfig`：时间、动作、探测、并发等预算。
- `WorkflowRunner(...)`：总调度器。

最后执行：
- `runner.run(task=args.task)`。

---

## 3. 运行前的数据准备（QuestionnaireState2）

对应文件：`questionnaire_state2.py`。

当前主流程使用的是“router + block”问卷模型：
- `routers`：路由问题（用于先判断当前页面与哪些 block 相关）。
- `blocks`：可填充问题块（每个 block 内含 questions）。
- `block_status`：运行时计数（命中次数、访问次数等）。

关键方法：
- `load_routers_and_blocks()`：读取 `questionnaire_routers.json` 和 `questionnaire_blocks.json`。
- `match_blocks_from_router_answers()`：把 LLM 的路由回答映射为命中的 block。
- `mark_blocks_hit()` / `mark_blocks_visited()`：更新运行时统计。
- `save_observation()`：落地每个状态的观测结果 JSON。

---

## 4. 主流程总控（WorkflowRunner.run）

对应文件：`workflow.py` 中 `WorkflowRunner.run()`。

### 4.1 建立初始状态

1. 确保目标应用在前台（`ensure_foreground`）。
2. 创建线程池（`ThreadPoolExecutor`）用于并发 LLM 调用。
3. 抓取首个权威快照（`_capture_and_process()`）。
4. 初始化探索上下文：
   - `cur_sig`（当前状态签名）
   - `entry_sig` / `restart_entry_sig`
   - `dfs_stack` / `dfs_via` / `parent_map`
5. 把入口状态写入状态图（`_graph_record_observation(..., entry=True)`）。
6. 为入口状态提交异步分析任务（`_schedule_state(...)`）。

### 4.2 主循环的固定顺序

每轮循环核心顺序是：
1. 检查停止条件（时间预算、动作上限、饱和/卡住等）。
2. `_drain_futures()`：回收已完成的 LLM future，刷新缓存和观测结果。
3. `foreground gate`：若跑到非目标前台，优先拉回。
4. `drift 检测`：页面在无动作情况下变化则强制重规划。
5. `NAV barrier`：优先等待 LLM1 导航结果，超时才允许 heuristic。
6. `overlay 处理`：先处理 dismiss/loading 等遮挡。
7. 候选探测 + 前进决策（probe/forward）。
8. 必要时走 beam 全局切换、DFS 回溯、recovery、restart+replay。
9. 成功动作后抓新快照并记录状态转移，再进入下一轮。

---

## 5. 快照处理（权威状态输入）

对应函数：`_capture_and_process()`。

这个函数是整个系统的“单一事实来源”，负责把设备状态转成可计算状态。

### 5.1 采集与归一化

- 调 `appium.capture_snapshot()` 同步拿到：
  - XML
  - screenshot(base64)
  - 设备信息（如 `pixelRatio`）
- 推断坐标缩放（`_infer_coord_scale`）。
- 读取前台 `package/activity`，并纳入状态签名空间。
- 进行状态栏裁剪与 XML 预处理，降低噪声。

### 5.2 UI 结构构建

1. `parse_xml_to_uist()`：XML -> 初始 UI 树。
2. `BaseUI.post_process_ui(...)` 或 `post_process_ui_uied_first(...)`：
   - 节点清洗
   - element_id 映射（`vid_map`）
   - OCR/UIED 等增强路径

### 5.3 多级签名与缓存

会计算：
- `state_sig`：主状态签名（决策/缓存主键）。
- `struct_sig`：结构签名（family 级归并）。
- `coarse_sig` / `fine_sig`：辅助调试与匹配。
- `screenshot_phash`：视觉近似匹配。

并维护 `snapshot_cache`，减少重复后处理开销。

---

## 6. 异步 LLM 流水线（调度与回收）

对应函数：`_schedule_state()` + `_drain_futures()`。

### 6.1 提交任务（schedule）

当一个新状态到达时：
- 若无缓存/无在途任务，提交 `LLM1 propose_navigation`。
- 同时提交 `LLM2-1 propose_router_answers`（新 block workflow）。
- 当 router 结果回来并匹配到 blocks 后，再提交 `LLM2-2 propose_blocks_fill`。

### 6.2 回收任务（drain）

`_drain_futures()` 负责：
- 将完成的 future 结果写回缓存。
- 记录事件（成功、取消、失败、超时、回退）。
- 对 router 结果执行本地 block 匹配。
- 保存 observation 文件（router 阶段、blocks_fill 阶段）。
- 更新 `state_update_counts` 等进展统计。

说明：
- 旧的 `topic_route/topic_fill` 接口在代码中保留，但当前 block_status 工作流里默认禁用。

---

## 7. 导航决策与动作执行

### 7.1 NAV barrier 机制

对应函数：`_wait_nav_or_fallback()`。

策略是：
- 先等 LLM1 导航结果。
- 等待期间周期性做 drift 检查。
- 只有 NAV 超时或进入 cooldown 才允许 heuristic fallback。

这样可避免“LLM未返回就乱点”。

### 7.2 overlay 优先处理

根据 `NavigationProposal.overlay_kind` 分流：
- `dismiss`：优先执行 `overlay_dismiss_actions`。
- `loading`：等待加载结束后重抓快照。
- 处理失败则进入 recovery，必要时 restart+replay。

### 7.3 候选动作与前进提交

系统会在候选动作中选择 forward 并执行：
- `_execute_action_sequence()` 执行动作链。
- 成功后立刻重新抓取快照。
- 用 `_graph_record_transition()` 记录 `src -> dst`。
- 更新 DFS 路径与“是否发现新状态”统计。

执行失败或抓图失败会触发恢复链路。

---

## 8. 探索完整性：DFS、Beam、恢复、回放

### 8.1 DFS 回溯

当当前状态已 exhausted：
- 回退到最近仍有未探索分支的祖先状态。
- 若祖先也无可用分支，则尝试全局 frontier。

### 8.2 Beam 全局切换

对应 `_maybe_beam_switch()`：
- 评估“本地继续前进”与“跳到全局更高价值状态”收益。
- 结合价值、路程成本、重启惩罚、边验证惩罚等打分。
- 满足 gain/cooldown/commit 约束才切换。

### 8.3 恢复与重启回放

对应 `_recover()`、`_restart_and_replay()`：
- 先 deterministic recovery。
- 再尝试 LLM3 `recover_state()` 规划恢复动作。
- 最后兜底重启 app 并按图路径回放。

---

## 9. 状态图（StateGraph）在流程中的作用

对应文件：`state_graph.py`。

StateGraph 维护：
- `Node`：状态节点（visit_count、overlay_kind、meta）。
- `Edge`：动作边（count、verified_ok/fail、last_verified_ts）。

主流程关键对接语义：
- `record_observation(sig)`：无前置动作时记录“观察到状态”。
- `record_transition(src, dst, action)`：记录动作导致的转移，并返回 `dst_was_new_at_discovery`。
- `annotate(sig, ...)`：附加元信息且不增加 visit_count。
- `shortest_action_path()`：为导航与 replay 提供路径。

---

## 10. 本次运行最终会产出什么

### 10.1 日志与追踪

- `app.log`：完整运行日志。
- 若设置 `--trace-dir`：输出 JSONL trace 和截图/XML/UI 树等资产。

### 10.2 问卷观测结果

在 `questionnaire_state2` 当前模式下：
- 每个状态会保存 router 和 blocks_fill 的 observation 文件。
- `block_status` 会累计命中与访问统计。

### 10.3 重要现状说明

当前代码里有明确注释：
- 新流程目前以“观测与采样”为主，尚未实现旧式“最终答案导出”闭环。
- 也就是说，系统会积累丰富证据和中间结果，但最终汇总导出仍需后续补齐。

---

## 11. 一次完整运行的简化时序

1. `main.py` 解析参数、初始化 GPT/Appium/QuestionnaireState2。
2. `WorkflowRunner.run()` 抓入口快照并记录入口状态。
3. 主循环中持续：
   - 回收异步分析结果。
   - 检测前台漂移/页面漂移。
   - 等 NAV、处理 overlay。
   - 探测候选、提交前进动作。
   - 记录状态图转移与统计。
4. 必要时执行 DFS 回溯、beam 跳转、recovery、restart+replay。
5. 达到停止条件后退出，输出日志、trace、observation、token 统计。

---

## 12. 关键代码定位（便于继续阅读）

- 入口与组装：`main.py`
- 总调度与主循环：`workflow.py`
- LLM 接口封装：`gpt_cls.py`
- 问卷状态（router/block）：`questionnaire_state2.py`
- 状态图：`state_graph.py`
- 设备控制：`appium_android.py`
- Trace 回调：`trace_callbacks.py`
